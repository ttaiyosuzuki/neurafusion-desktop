# SAFETY — 脅威モデル（下書き・未公開）

- 作成: 2026-09-15
- 状態: **下書き。オーナーの目視確認が1回済むまで公開しない**（S22 裁定）
- 公開先（予定）: `/fork` の SAFETY 節、および独立ページ
- 前提となる条文: 利用規約 第16条の2（デスクトップクライアントによる端末内の読取りと能力の推定）

> **なぜこの文書が要るのか**
> NeuraFusion は「あなたのPCの中を読む」と言っています。
> それを言う製品が、何を読み・何を送らず・誰が見られて・取り消したら何が消えるのかを
> 書いていない状態は通りません。契約でも法務レビューでも、ここが最初に見られます。

---

## (a) 何を読み、何を送るか

### 読むもの（端末の中だけ。あなたがオンにした対象に限る）

| 対象 | 読む内容 | 既定 |
| --- | --- | :---: |
| ローカルのファイル | 種類・構成・更新の頻度、および**中身** | オフ |
| アプリケーション | どれを、どれくらい、どう使っているか | オフ |
| 連絡手段（メール / Slack / Teams / Discord / LINE / Instagram / X のDM / Copilot） | やり取りの相手・頻度・応答速度・継続期間、および**本文** | オフ |

**既定はすべてオフです。** オンにしない限り、1バイトも読みません。

### 読まないもの

- **カメラ**（S22 裁定で読まないと決定）
- **位置情報**（同上）

分身の測定に要らないうえ、端末内を読む製品にとっては攻撃面と誤解だけが増えるためです。

### 送るもの（端末の外に出るのはこれだけ）

```
patternId        … 端末内で作った「型」の識別子。文字列そのものではない
集計数値          … 分野ごとの作業頻度・応答速度・関係の強度（0〜1）
```

### 送らないもの

- 本文・件名
- ファイルの中身・ファイル名
- **相手の氏名・メールアドレス・アカウントID等の識別子**
  （相手が NeuraFusion の利用者でない場合も同じです）

### 全数検査

送信するペイロードは、**1件ずつ全数を機械検査**してから出します。

- 検査で見るもの: 生の文字列が混ざっていないか、相手を特定できる値が無いか
- **検査に落ちたペイロードは送らず、端末内に留めて破棄します**
- 検査の実装は `computeGraphPageRank` の入力型が境界になっています。
  型として生値を受け取れないので、そもそも送信経路に載りません

> ⚠️ 現時点の実装状況: **デスクトップクライアント本体は未作成**です。
> ここに書いた読み取りは、まだ1つも動いていません。設計と条文が先に立っている状態です。

---

## (b) 鍵の置き場

| 項目 | 方針 |
| --- | --- |
| APIキー・認証情報の保存先 | **端末のOSの安全な保管庫**（macOS Keychain / Windows DPAPI / Linux Secret Service） |
| 平文ファイルへの保存 | **しません** |
| 当社サーバでの預かり | **しません**。当社は利用者の鍵を1つも持ちません |
| 当社から鍵が漏れる可能性 | **持っていないので、漏れません** |

利用者が自分のアカウント（Claude / OpenAI / Gemini / DeepSeek / Kimi 等）で使う形にしているのは、
この「持たない」を成立させるためです。

**脅威と対応**

| 脅威 | 対応 |
| --- | --- |
| 端末が盗まれる | OSの保管庫はOSのロックに従う。当社側では何もできない（正直に書く） |
| マルウェアが保管庫を読む | OSの権限モデルに依存。当社の実装では防げない |
| 当社の障害・侵害で鍵が漏れる | **該当しない**（持っていない） |

---

## (c) 取り消しと削除の動作

### 取り消しの単位

- 連絡手段ごと（メールだけ止める、Slack だけ止める）
- フォルダごと（この階層だけ読ませない）

### 取り消したとき、何が消えるか

```
1. 端末内のその対象由来の特徴量      → 即時削除
2. 当社サーバのその対象由来の patternId と数値 → 速やかに削除
3. 1と2から導かれた学習結果          → 削除が及ぶ
```

**3が重要です。** 「元データは消したが、学習済みモデルには残っている」を認めません。
取り消しは学習成果物にも及びます（利用規約 第16条の2 第5項）。

### 監査

いつ・どの対象をオンにし、どの patternId が送られたかの記録を、
**本人がクライアント上でいつでも確認できます**。

---

## (d) 一人で見られない設計

### 原則

**当社の従業者が、個人を特定できる形で第16条の2 第1項の情報を単独で閲覧することはできません。**

### 例外が必要になる場合の手順

```
1. 閲覧の必要性を記録する（何のために、どの範囲を）
2. 複数名の承認を得る（1人では通らない）
3. 対象となる利用者に通知する
4. 承認と閲覧の記録を保存する
```

この4つが揃わない閲覧は行いません。

### 将来（まだ実装していない）

トークンによる権限の分散を検討しています。
**現時点では検討段階であり、実装はありません。** 実装する前にこの文書を更新します。

> ⚠️ 「DAO化」「トークン分散」は**将来目標**です。
> 現在の保護は上の4手順（人の手続き）によるものであって、技術的な強制ではありません。
> ここを曖昧にしないために、はっきり書いておきます。

---

## (e) 事故時の対応

### 窓口

| 種別 | 連絡先 |
| --- | --- |
| 脆弱性の報告 | （**要記入**: security@ のアドレスをエイリアスで用意する） |
| 事故の問い合わせ | （**要記入**） |
| 一般の問い合わせ | `/contact` |

> ⚠️ **オーナーへ**: ここは空欄です。Google Workspace のエイリアス（1ユーザー30個まで無料）で
> `security@neurafusion.jp` を作れば埋まります。**窓口の無い脅威モデルは形だけになります。**

### 事故が起きたときの動き

```
1. 止める        … 該当する経路を止める。送信を含む
2. 範囲を測る    … 何件・どの対象・どの期間かを記録から出す
3. 知らせる      … 影響を受けた利用者へ通知する。事実だけを書く
4. 直す          … 原因を直し、再発しない形にする
5. 残す          … 何が起きて何をしたかを記録に残す
```

### 通知の方針

- **分かっていないことを「問題ありません」と書きません**
- 範囲が確定していない段階でも、**確定していないという事実を伝えます**
- 件数が後から増えた場合、増えたことを伝えます（黙って上書きしません）

---

## 通信の経路

| 経路 | 出るもの | 出ないもの |
| --- | --- | --- |
| 端末 → 当社サーバ | patternId・集計数値 | 生値・相手の識別子 |
| 端末 → モデル提供者（Claude / OpenAI 等） | 利用者が明示的に送った依頼の内容 | 端末内の走査結果 |
| 当社サーバ → 第三者 | **なし**（販売しません） | — |

利用者が自分の鍵で使うモデル提供者への通信は、**当社を経由しません**。
当社のサーバはその内容を見られません。

---

## この文書の限界（正直に）

1. **デスクトップクライアント本体が未実装**です。ここに書いた保護の多くは、
   設計と条文であって、動いているコードではありません。
2. **形式的検証は行っていません**。
3. **第三者によるセキュリティ監査は受けていません**。
4. 上記3点が変わったら、この文書を更新します。

---

## オーナー確認チェックリスト（公開前に1回）

```
□ (a) 読む対象の一覧に、実際に読むつもりのないものが混ざっていないか
□ (a) カメラ・位置情報を「読まない」と書いてよいか（S22裁定どおりか）
□ (b) 鍵の保管先の記述が、実装の予定と一致しているか
□ (c) 「学習結果にも削除が及ぶ」を約束してよいか（技術的に守れるか）
□ (d) 複数名承認の「複数名」が誰なのか、運用上決まっているか
□ (e) security@ のアドレスを用意したか ← 未了
□ 「限界」の4点を公開してよいか（隠したくなるが、隠すと後で効く）
```

**⚠️ (c) と (d) は約束です。** 書いた以上、守れないと契約違反になります。
実装の見通しが立たない項目があれば、公開前に文言を弱めてください。
