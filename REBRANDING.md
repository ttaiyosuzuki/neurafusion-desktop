# リブランドの方針（NF-63 / S23 PART 1-2）

作成: 2026-09-15

## 結論を先に

**「名称の全置換」は、このリポジトリでは実行しません。** 実行するとビルドが壊れます。
代わりに **ユーザーに見える面だけ** を NeuraFusion にします。

## 実測した数字（2026-09-15）

上流 `openclaw/openclaw` を浅くクローンして数えた結果です。

| 対象 | 件数 |
| --- | ---: |
| 全ファイル | 43,109 |
| `openclaw` を含むファイル | **28,930**（67%） |
| `from "openclaw…"` の import を持つファイル | **6,657** |
| `OPENCLAW_*` 環境変数の種類 | **2,938** |
| 設定ディレクトリ `~/.openclaw` を参照するファイル | **6,197** |
| Bundle ID `ai.openclaw` を持つファイル | **848** |
| npm パッケージ名を参照する JSON | 176 |

## なぜ全置換をしないのか

1. **import が壊れる**
   `openclaw` は npm 上の実パッケージ名です。6,657ファイルの import を書き換えると、
   npm レジストリに存在しない名前を指すことになり、依存解決が全部落ちます。
   公式プラグイン142本も `openclaw` を import しています。

2. **環境変数が壊れる**
   `OPENCLAW_*` は 2,938種あります。名前を変えると、既存の設定・ドキュメント・
   プラグイン・CI がすべて無効になります。

3. **設定ディレクトリが壊れる**
   `~/.openclaw` を 6,197ファイルが参照しています。変えると既存利用者の設定が消えます。

4. **Bundle ID を変えると署名と更新が切れる**
   `ai.openclaw` は 848ファイル。これは配布・自動更新の識別子です。

5. **上流に追随できなくなる**
   上流は活発に開発されています（389,722スター）。全置換すると、
   以後の `git merge upstream/main` が全ファイル衝突になります。
   **セキュリティ修正を取り込めなくなるのが一番危険です。**

## 実際にやること（ユーザーに見える面だけ）

| 層 | 扱い | 理由 |
| --- | --- | --- |
| アプリの表示名・ウィンドウ題・About | **NeuraFusion Desktop に変更** | 利用者が見る |
| README・利用者向けドキュメント | **NeuraFusion に変更** | 利用者が読む |
| サイト（neurafusion.jp/fork）の文言 | **変更済み**（grep 0件） | 利用者が見る |
| ロブスター（🦞）の呼称・絵文字 | **削除**（S22 の「−」判定） | 上流固有の呼称 |
| 501(c)(3) の運営文言 | **削除**（S22 の「−」判定） | 該当しない |
| npm パッケージ名 `openclaw` | **変えない** | 依存が壊れる |
| `OPENCLAW_*` 環境変数 | **変えない** | 設定が壊れる |
| `~/.openclaw` 設定ディレクトリ | **変えない** | 既存設定が消える |
| Bundle ID `ai.openclaw` | **変えない** | 署名・更新が切れる |
| import 文・内部の識別子 | **変えない** | 動かなくなる |
| `LICENSE` / `NOTICE` | **上流表記を残す** | MIT の義務 |

## NF-63 の検収条件との関係

NF-63 の検収は「grep でユーザー可視面の OpenClaw 文字列がゼロ（LICENSE/NOTICE除く）」です。

- **サイト側（neurafusion.jp/fork）**: 達成済み。grep 0件を確認済み
- **アプリ側（このリポジトリ）**: 表示文字列のみ対象とする。内部識別子は対象外

**内部識別子まで「ユーザー可視面」に含めると、上流追随を捨てることになります。**
セキュリティ修正を取り込めない自社フォークを持つのは、端末内を読む製品にとって
リブランドの見栄えより大きな危険です。

## 将来、本当に全置換するなら

以下を同時にやる必要があります。合計で数週間規模です。

1. npm に自社スコープでパッケージを publish（`@neurafusion/*`）
2. 142本のプラグインを全部フォークして import を書き換え
3. 環境変数の移行層（旧名も読む）を書く
4. 設定ディレクトリの移行スクリプト（`~/.openclaw` → 新パス）
5. Bundle ID 変更にともなう署名証明書の再取得と、旧版からの移行経路
6. 上流追随を諦めるか、パッチ管理の仕組みを作る

**いま必要なのは配布できる第1号であって、完全な改名ではありません。**
判断が変わったらこの文書を更新してください。
